"""The Intelligence plane (Milestone 1).

The second, NEW plane of TradeOS. Where the Signal plane (SEC convergence) forbids the model from
interpreting at all — it may only rephrase computed math — this plane lets an AI *interpret* sourced
information: it reads real news and explains why it may matter. It stays honest by construction:

  * Every number that ranks or rates (impact, confidence) is DETERMINISTIC, not the model's opinion.
  * The model writes only the interpretive prose, and that prose is guarded — no advice, no number the
    source didn't state, no ticker the item isn't about — and always framed "AI analysis, not advice."
  * With no key or on any failure, a deterministic template is used instead; the product never depends
    on the model being up.
"""
