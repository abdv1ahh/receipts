"""Entity resolution: one asset recognized across a Form 4, a 13D, and a 13F; one
institution recognized across every document it files. The rule that governs the whole
module is never guess — a record that cannot be resolved is stored unresolved and shown
flagged, never attached to a plausible-looking wrong entity."""
